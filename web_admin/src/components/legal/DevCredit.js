export default function DevCredit({ className = '' }) {
  return (
    <span className={`text-[9px] text-slate-500/35 font-normal tracking-wide select-none ${className}`}>
      Cavila
    </span>
  );
}